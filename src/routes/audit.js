const express=require('express');const pool=require('../db/pool');const {requireAuth,requireRole}=require('../middleware/auth');const router=express.Router();router.get('/',requireAuth,requireRole('ADMIN'),async(req,res,next)=>{try{const limit=Math.min(Number(req.query.limit||50),200);const r=await pool.query('SELECT id,user_id,action,entity,entity_id,before_data,after_data,ip,user_agent,created_at FROM audit_logs ORDER BY created_at DESC LIMIT $1',[limit]);res.json({data:r.rows});}catch(e){next(e)}});module.exports=router;
